{
    "version": "0.2.0",
    "configurations": [{
        "type": "chrome",
        "request": "launch",
        "name": "Launch Program",
        "webRoot": "${workspaceFolder}",
        "url": "http://localhost:8080"
    }]
}